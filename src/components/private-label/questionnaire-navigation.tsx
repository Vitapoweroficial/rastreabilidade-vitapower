export function QuestionnaireNavigation({ currentStep, totalSteps, onBack, onNext, onPrint, onSubmit, submitting }: { currentStep: number; totalSteps: number; onBack: () => void; onNext: () => void; onPrint: () => void; onSubmit: () => void; submitting: boolean }) {
  const last = currentStep === totalSteps - 1;
  return <div className="flex flex-col gap-3 sm:flex-row sm:justify-between"><button type="button" className="btn-secondary" onClick={onBack} disabled={currentStep === 0 || submitting}>Voltar</button><div className="flex gap-3"><button type="button" className="btn-secondary" onClick={onPrint} disabled={submitting}>Gerar PDF / Imprimir</button><button type="button" className="btn-primary" disabled={submitting} onClick={last ? onSubmit : onNext}>{submitting ? "Enviando..." : last ? "Enviar briefing para Vita Power" : "Próxima etapa"}</button></div></div>;
}
